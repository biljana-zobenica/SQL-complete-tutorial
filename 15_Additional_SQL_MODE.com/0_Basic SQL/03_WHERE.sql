SELECT *
  FROM tutorial.us_housing_units
 WHERE month = 1;
 
 /* The SQL WHERE clause works in a plain-English way: 
 the above query does the same thing as SELECT * FROM 
 tutorial.us_housing_units, except that the results will
 only include rows where the month column contains the value 1.*/